import flixel.FlxG;
import utf.Paths;
import utf.objects.dialogue.typers.Typer;

class DefaultTyper extends Typer
{
	public function new():Void
	{
		super('default');

		fontName = Paths.font('DTM-Mono', false);
		fontSize = 32;
		typerSounds = [{sound: Paths.sound('txt1', false), volume: 1}];
		typerLPS = 0.25;
	}
}
