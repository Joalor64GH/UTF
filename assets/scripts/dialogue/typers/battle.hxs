import flixel.FlxG;
import utf.Paths;
import utf.objects.dialogue.typers.Typer;

class BattleTyper extends Typer
{
	public function new():Void
	{
		super('battle');

		fontName = Paths.font('DTM-Mono', false);
		fontSize = 32;
		typerSounds = [{sound: Paths.sound('txt2', false), volume: 0.86}];
		typerLPS = 0.06;
	}
}
