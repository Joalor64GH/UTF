import utf.Paths;
import utf.objects.room.Object;

class SolidExWideObject extends Object
{
	public function new():Void
	{
		super('solidexwide');

		loadGraphic(Paths.sprite(objectID));
		scale.set(2, 2);
		updateHitbox();

		immovable = true;
		solid = true;
	}
}
