import utf.AssetPaths;
import utf.objects.room.Object;

class DoorBObject extends Object
{
	public function new():Void
	{
		super('doorB');

		loadGraphic(AssetPaths.sprite(objectID));
		scale.set(2, 2);
		updateHitbox();

		active = false;
		immovable = true;
		solid = false;
	}
}
