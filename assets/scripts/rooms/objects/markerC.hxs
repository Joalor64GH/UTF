import utf.Paths;
import utf.objects.room.Object;

class MarkerCObject extends Object
{
	public function new():Void
	{
		super('markerC');

		loadGraphic(Paths.sprite(objectID));
		scale.set(2, 2);
		updateHitbox();

		immovable = true;
		solid = false;
	}
}
