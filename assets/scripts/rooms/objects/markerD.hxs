import utf.Paths;
import utf.objects.room.Object;

class MarkerDObject extends Object
{
	public function new():Void
	{
		super('markerD');

		loadGraphic(Paths.sprite(objectID));
		scale.set(2, 2);
		updateHitbox();

		immovable = true;
		solid = false;
	}
}