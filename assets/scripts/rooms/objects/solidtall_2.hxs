import utf.AssetPaths;
import utf.objects.room.Object;

class SolidTall2Object extends Object
{
	public function new():Void
	{
		super('solidtall_2');

		loadGraphic(AssetPaths.sprite(objectID));
		scale.set(2, 2);
		updateHitbox();

		active = false;
		immovable = true;
		solid = true;
	}
}
