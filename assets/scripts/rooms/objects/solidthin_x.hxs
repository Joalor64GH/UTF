import utf.AssetPaths;
import utf.objects.room.Object;

class SolidThinXObject extends Object
{
	public function new():Void
	{
		super('solidthin_x');

		loadGraphic(AssetPaths.sprite(objectID));
		scale.set(2, 2);
		updateHitbox();

		active = false;
		immovable = true;
		solid = true;
	}
}
