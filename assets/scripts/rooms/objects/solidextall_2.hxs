import utf.Paths;
import utf.objects.room.Object;

class SolidExTall2Object extends Object
{
	public function new():Void
	{
		super('solidextall_2');

		loadGraphic(Paths.sprite(objectID));
		scale.set(2, 2);
		updateHitbox();

		immovable = true;
		solid = true;
	}
}
