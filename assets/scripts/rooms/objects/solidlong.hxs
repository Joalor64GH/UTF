import utf.backend.AssetPaths;
import utf.objects.room.Object;

class SolidLong extends Object
{
	public function new():Void
	{
		super('solidlong');

		loadGraphic(AssetPaths.sprite(objectID));
		scale.set(800, 2);
		updateHitbox();
		immovable = true;
		solid = true;
	}
}
