import utf.Paths;
import utf.objects.room.Object;

class SolidLongObject extends Object
{
	public function new():Void
	{
		super('solidlong');

		loadGraphic(Paths.sprite(objectID));
		scale.set(800, 2);
		updateHitbox();

		immovable = true;
		solid = true;
	}
}
